# Aegis Project Records

This directory stores durable planning, baseline, and execution records for Theme-Lab.

Theme-Lab's source of truth remains the project files under `config/`, `samples/`,
`tools/`, `tests/`, `derived/`, `reference/captures/`, and `findings/`. Aegis records
explain why changes are made and how they should be verified.
